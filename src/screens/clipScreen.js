import React, { Component } from "react";
import { View, StyleSheet } from "react-native";

class ClipScreen extends Component {
  componentDidMount = async () => {
    // TODO fetch this.props.route.params.clip_id
    // clips feed
  };
  render = () => {
    return null;
  };
}

export default ClipScreen;
